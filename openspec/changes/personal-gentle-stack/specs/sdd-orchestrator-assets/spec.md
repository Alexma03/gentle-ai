# SDD assets

## MODIFIED Requirements

### Requirement: Bounded Review Contract Parity Across Catalog Agents
Five retained templates MUST render a bounded-review contract preserving immutable final-candidate review, one refuter, scoped validation, and user-owned runtime selection; redundant task-level authority MUST be removed.
(Previously: parity covered 16 IDs.)

#### Scenario: Template parity
- GIVEN five retained templates
- WHEN guidance is inspected
- THEN each has the contract and no extra authority budget.
